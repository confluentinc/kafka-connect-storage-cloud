Summary: Kafka Connect S3 connector for copying data between Kafka and Amazon Simple Storage Service (S3).
Name: confluent-kafka-connect-s3
Version: ##RPMVERSION##
Release: ##RPMRELEASE##
Group: Applications/Internet
License: Confluent Community License
Source0: confluent-kafka-connect-s3-%{version}.tar.gz
URL: http://confluent.io
BuildRoot: %{_tmppath}/%{name}-%{version}-root
Vendor: Confluent, Inc.
Packager: Confluent Packaging <packages@confluent.io>
BuildArch: noarch

Requires: confluent-kafka-connect-storage-common

%description

Kafka Connect S3 is a connector for copying data between Kafka and Amazon Simple Storage Service (S3).

%define __jar_repack %{nil}

%pre

exit 0

%post

%preun

%postun

%prep

%setup

%build

%install

# Clean out any previous builds not on slash
[ "%{buildroot}" != "/" ] && %{__rm} -rf %{buildroot}

%{__mkdir_p} %{buildroot}
%{__cp} -R * %{buildroot}
# The spec file gets included, get rid of it
%{__rm} %{buildroot}/confluent-kafka-connect-s3.spec

%files
%defattr(-,root,root)

%doc
/usr/share/java/kafka-connect-s3
/usr/share/doc/kafka-connect-s3

%config(noreplace) /etc/kafka-connect-s3

%clean
#used to cleanup things outside the build area and possibly inside.
[ "%{buildroot}" != "/" ] && %{__rm} -rf %{buildroot}

%changelog
* Thu Jan 26 2017 Confluent Packaging <packages@confluent.io>
- Initial import
